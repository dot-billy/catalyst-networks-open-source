from django.urls import path

app_name = 'organizations_api'

# Organization-scoped admin APIs are intentionally disabled.
# Membership and invitation management remain web-only administration flows.
urlpatterns = [
]